import { index, type RouteConfig } from "@react-router/dev/routes";
import { routesConfig } from "./configuration/routes.config";

export const routes: RouteConfig = [...routesConfig];

export default routes;
